import React from 'react';

// Error functional component definition
const Error = () => (

    <div className="error">
        <p>Invalid city name</p> {/* Error message displayed to the user */}
    </div>
);

export default Error;
